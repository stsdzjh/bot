spring:
  application:
    name: bot-auth
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_HOST:192.168.1.13}:${NACOS_APPLICATION_PORT:8848}
        file-extension: yml
        group: DEFAULT_GROUP
